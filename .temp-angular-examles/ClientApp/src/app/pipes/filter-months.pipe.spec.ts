import { FilterMonthsPipe } from './filter-months.pipe';

describe('FilterMonthsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMonthsPipe();
    expect(pipe).toBeTruthy();
  });
});
